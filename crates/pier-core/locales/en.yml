# English — source locale for the Pier admin panel.
#
# This is the canonical catalog: every user-facing string lives here as a key.
# Other languages are added later as sibling files (`ru.yml`, `zh-CN.yml`, …);
# missing keys fall back to this file (`i18n!(.., fallback = "en")`).
#
# Key convention: <area>.<screen>.<element>. Shared words go under `common.*`,
# sidebar navigation under `nav.*`. Interpolation uses `%{name}` placeholders;
# the MiniJinja `t()` bridge fills them from template kwargs.

# Words/labels reused across many screens.
common:
  logout: Logout
  view_all: View all
  loading: Loading...
  no_description: No description
  cancel: Cancel
  copy: Copy
  optional: (optional)
  # Client-side toast / fetch messages emitted from shared scripts.
  copied: Copied!
  copy_failed: Copy failed
  request_failed: Request failed

# Sidebar navigation (labels + collapsed-sidebar tooltips share these keys).
nav:
  dashboard: Dashboard
  projects: Projects
  servers: Servers
  sources: Sources
  s3: S3 Storages
  registries: Registries
  packages: Packages
  domains: Domains
  proxy: Proxy
  networks: Networks
  team: Team
  tasks: Tasks
  schedules: Schedules
  settings: Settings
  logs: Logs
  updates: Updates
  notifications: Notifications
  # Sidebar section headers.
  group:
    integrations: Integrations
    networking: Networking
    system: System

# Shared chrome rendered by base.html.
layout:
  support_pier: Support Pier
  deploy_in_progress: In Progress
  # Deploy badge count: "N deployment(s)". JS picks singular/plural by count.
  deployment_one: deployment
  deployment_other: deployments

# Login + two-factor screen (login.html).
login:
  title: Login
  sign_in: Sign in
  two_factor: Two-factor authentication
  username: Username
  # Example username shown as the field placeholder (the default account name).
  username_placeholder: admin
  password: Password
  signing_in: Signing in...
  totp_hint: Enter the 6-digit code from your authenticator app, or a recovery code.
  verify: Verify
  verifying: Verifying...
  back: ← back
  error:
    login_failed: Login failed
    invalid_code: Invalid code
    request_failed: Request failed

# Dashboard (dashboard.html).
dashboard:
  title: Dashboard
  subtitle: System overview and resource usage
  architecture: Architecture
  cpu_usage: CPU Usage
  memory: Memory
  disk: Disk
  containers: Containers
  # Suffixes/labels assembled in Alpine x-text expressions.
  cores: cores
  running: running
  stopped: stopped
  docker_engine: Docker Engine
  version: Version
  api_version: API Version
  storage_driver: Storage Driver
  images: Images
  projects: Projects
  no_projects: No projects yet
  create_first_project: Create your first project
  system: System
  hostname: Hostname
  os: OS
  uptime: Uptime
  servers: Servers
  memory_by_container: Memory Usage by Container
  loading_stats: Loading stats...
  no_running_containers: No running containers
  disk_usage: Disk Usage
  volumes: Volumes
  build_cache: Build Cache
  total_disk_usage: Total Docker Disk Usage

# Shared auth-page chrome (auth_base.html).
auth:
  tagline: Deploy Anything. Own Everything.

# First-run admin setup (setup.html).
setup:
  title: Setup
  proxy_error_title: Reverse-proxy (Traefik) failed to start
  proxy_error_hint: "This is often caused by a Docker Hub rate-limit on shared-IP VPSes. To fix it manually, run on the server:"
  proxy_error_after: "After the restart, Pier brings Traefik back up automatically. This banner disappears on /setup."
  create_admin: Create Admin Account
  subtitle: First-time setup. Create the administrator account.
  username: Username
  username_placeholder: admin
  email: Email
  password: Password
  generate_password: Generate strong password
  password_placeholder: Min. 12 characters, mixed
  confirm_password: Confirm password
  confirm_placeholder: Re-enter password
  passwords_no_match: Passwords do not match
  creating: Creating...
  strength:
    very_weak: Very weak
    weak: Weak
    fair: Fair
    good: Good
    strong: Strong
  pw_copied: Password copied to clipboard. Save it now — it will not be shown again.
  pw_generated: Password generated. Copy it from the field — it will not be shown again.
  fix_fields: Please fix the highlighted fields before continuing.
  setup_failed: Setup failed

# CLI authorization page for `npm login --auth-type=web` (cli_login.html).
cli_login:
  title: Authorize CLI
  request_desc: A command-line tool is requesting a token to publish or install npm packages from this registry.
  authorizing_as: Authorizing as
  hostname: Hostname
  source_ip: Source IP
  user_agent: User-Agent
  expires_in: Expires in
  expired_short: expired
  token_name: Token name
  token_name_placeholder: laptop, CI runner, …
  authorize: Authorize
  issuing: Issuing token…
  authorized: CLI authorized
  authorized_hint: "Your terminal should pick up the token within a few seconds. If it doesn't, copy the token below and paste it into <code class=\"font-mono\">.npmrc</code>."
  token_fallback: Token (fallback)
  token_once: This is the only time the token is shown here. Once the CLI fetches it, the entry is cleared.
  back_to_packages: Back to Packages
  session_expired: Session expired
  expired_hint: "Re-run <code class=\"font-mono\">npm login --auth-type=web --registry=…</code> in your terminal to start a fresh session."
  auth_failed: Authorization failed
  clipboard_failed: Clipboard copy failed — select and copy manually.

# Invitation acceptance (invitations/accept.html).
invite:
  title: Accept Invitation
  verifying: Verifying invitation…
  invalid_title: Invitation not valid
  invalid_desc: This link has expired, been used, or never existed. Ask whoever invited you to send a new one.
  back_to_login: Back to login
  welcome: Welcome to Pier
  invited_as: You were invited as
  choose_credentials: Choose a username and password to finish setting up your account.
  username: Username
  password: Password
  password_hint: At least 12 characters, not derived from your username or email.
  create_account: Create account
  working: Working…
  created_title: Account created
  created_desc: Your account is ready. Sign in with the credentials you just chose.
  go_to_login: Go to login
  accept_failed: Failed to accept invitation

# Projects list (projects/list.html).
projects_list:
  title: Projects
  heading: Projects
  subtitle: Manage projects and standalone resources
  all_servers: All servers
  this_server: This server
  new: New
  menu_project: Project
  menu_project_desc: Create a logical group for resources
  menu_resource: Resource
  menu_resource_desc: Deploy a database, application, or service
  federated: federated
  managed_on: Managed on
  opens_server_ui: Opens that server's UI.
  ports_label: 'Ports:'
  created_label: 'Created:'
  synced_label: 'Synced:'
  standalone_resources: Standalone Resources
  open_prefix: Open
  public: public
  empty_title: No projects yet
  empty_subtitle: Create a project or deploy a resource to get started
  modal_title: New Project
  name_label: Name
  description_label: Description
  description_placeholder: Optional description
  port_start_label: Port Start
  port_end_label: Port End
  create: Create
  never: never
  ago: ago
  peer: peer
  peer_plural_suffix: s
  offline: offline
  synced: Synced
  sync_pending: Sync pending
  error: error
  unknown: unknown
  last_sync: last sync
  project_created: Project created

# Project detail (projects/detail.html).
projects_detail:
  title: Project
  edit: Edit
  delete: Delete
  name: Name
  description: Description
  port_start: Port Start
  port_end: Port End
  save_changes: Save Changes
  port_range: Port Range
  not_set: Not set
  created: Created
  services: Services
  new_service: New Service
  open: Open
  public: public
  no_services: No services in this project
  registry_credentials: Registry credentials
  registry_credentials_desc: Project-scoped credentials override global ones for the same registry host.
  add: Add
  registry: Registry
  username: Username
  label: Label
  actions: Actions
  test: Test
  no_project_creds: 'No project-scoped credentials. Falls back to <a href="/settings/registries" class="text-blue-500 hover:underline">global credentials</a>.'
  add_registry_credential: Add registry credential
  registry_host: Registry host
  password_token: Password / token
  saving: Saving…
  delete_project: Delete Project
  has_services_prefix: This project has
  service_singular: service
  service_plural_suffix: s
  delete_services_first: Delete each service before you can remove the project.
  close: Close
  permanent_delete_warning: This will permanently delete the project.
  type_to_confirm_prefix: Type
  type_to_confirm_suffix: 'to confirm:'
  account_password: Account password
  enter_password_placeholder: Enter your password
  deleting: Deleting…
  toast_load_failed: Failed to load project
  confirm_delete_cred_prefix: 'Delete credentials for '
  toast_cred_added: Credential added
  toast_connection_ok: Connection OK
  toast_failed_prefix: 'Failed: '
  unknown: unknown
  toast_project_updated: Project updated
  toast_project_deleted: Project deleted

# Servers list (servers/list.html).
servers_list:
  title: Servers
  heading: Servers
  subtitle: "Local core, remote agents, and federated peer cores — all in one place."
  add_server: Add Server
  add: Add
  edit: Edit
  delete: Delete
  save: Save
  create: Create
  done: Done
  working: Working…
  rename: Rename
  test: Test
  remove: Remove
  revoke: Revoke
  revoked: revoked
  local: local
  peer: peer
  mesh_network: Mesh Network
  mesh_disabled_desc: WireGuard tunnel between every Pier node. Disabled.
  mesh_peers: peers
  mesh_on: on
  enable_mesh: Enable Mesh
  disable: Disable
  apply: Apply
  applying: Applying…
  apply_title: Re-run install + keypair + apply on every peer
  preflight_title: Mesh preflight
  preflight_desc: 'Every node needs <span class="font-mono">pier-net-helper</span> running before mesh can come up. Run the command below on each missing node, then click <em>Re-check</em>.'
  helper_online: helper online
  helper_missing: helper missing
  preflight_run_command: "Run this command as root on every red node above:"
  recheck: Re-check
  checking: Checking…
  preflight_unreachable: preflight endpoint unreachable
  service_dns: Service DNS
  service_dns_empty: No mappings. Register a logical name so stacks can refer to a service by role instead of node.
  service_dns_count: mapping(s) — stacks redeploy automatically on change
  new_plus: + New
  edit_prefix: Edit
  service_dns_new_title: New service-DNS mapping
  service_dns_form_desc: 'A name like <span class="font-mono">db</span> becomes <span class="font-mono">db.mesh</span> in every stack. Lowercase, [a-z0-9-], 1-31 chars.'
  name_label: Name
  target_server_label: Target server
  select_option: "— select —"
  local_suffix: (local)
  meshed_only_hint: Only meshed servers actually inject; non-mesh peers are silently skipped at deploy time.
  port_label: Port
  federation_tokens: Federation Tokens
  fed_tokens_empty: No tokens issued. Mint one to let a remote primary pier-core deploy stacks here.
  fed_tokens_count: token(s) issued
  new_token_plus: + New Token
  never_used: never used
  fed_token_create_title: Mint federation token
  fed_token_create_desc: 'The plaintext token is shown <strong>once</strong>. Copy it into the primary''s "Connect to peer" form right after creation.'
  label_label: Label
  label_hint: Any identifier for the primary that will use this token.
  token_for: Token for
  reveal_warning: Copy this now — Pier never shows it again. After closing this dialog only the prefix is visible.
  reveal_instructions: 'On the primary pier-core: go to <span class="font-mono">/servers</span> → this peer''s row → paste this value into the <strong>Federation token</strong> field.'
  copied_confirm: I've copied it
  mesh_settings: Mesh settings
  mesh_settings_desc: Only editable while mesh is disabled — changing subnet on a live mesh would orphan every assigned IP.
  subnet_label: Subnet (CIDR)
  subnet_hint: /30 or shorter prefix. /24 holds 254 nodes.
  listen_port_label: Listen port
  listen_port_hint: UDP. Must be open in the firewall on every node.
  keepalive_label: PersistentKeepalive (sec)
  keepalive_hint: 0 disables keepalive. Use ≥25 if any node is behind NAT.
  cpu: CPU
  ram: RAM
  disk: Disk
  hostname: Hostname
  host: Host
  uptime: Uptime
  os: OS
  url: URL
  last_heartbeat: Last Heartbeat
  never: Never
  view_snapshot: View snapshot
  open_panel: Open panel
  kind_agent: Agent
  kind_agent_desc: Lightweight worker, metadata stays here
  kind_peer: Peer core
  kind_peer_desc: Autonomous pier-core with own DB
  name_placeholder: Production Server
  peer_token_instructions: "On the target server: Settings → External Access → Issue Token, then paste the token below."
  peer_token_label: Peer Token
  peer_token_placeholder: token issued by remote core
  agent_created: Agent created
  agent_created_desc: Run the install command on the remote server
  install_command_expires: Install command — bootstrap token expires
  install_command_note: The command exchanges the bootstrap for a long-term token and starts the agent. This bootstrap is one-shot — you won't see it again. If the install fails, delete the server and create it again to issue a fresh bootstrap.
  expires_soon: soon
  expires_now: now (refresh — bootstrap has expired)
  expires_lt_minute: in less than a minute
  expires_1_minute: in 1 minute
  expires_in_prefix: in
  expires_minutes_suffix: minutes
  ago: ago
  warn_ipv6_only: This host looks IPv6-only but this server has no public IPv6 — handshake will fail. Add a v6 address on this server or use a v4 host.
  warn_ipv4_no_public: This host is IPv4 but this server has no public IPv4. The agent may reach core fine outbound, but mesh fallback to public IPv4 will not work.
  toast_name_required: Name is required
  toast_agent_created: Agent created — run the install command
  toast_peer_registered: Peer registered
  toast_server_online: Server is online
  remove_confirm_prefix: Type
  remove_confirm_suffix: "to confirm removal:"
  toast_name_mismatch: Name does not match
  toast_server_removed: Server removed
  toast_renamed: Renamed
  configure_aborted: Configure aborted on a peer. Check the peer rows below for details.
  toast_mesh_active: Mesh active
  toast_mesh_partial: Mesh partially configured
  toast_enable_mesh_failed: Enable mesh failed
  toast_command_copied: Command copied
  toast_create_failed: Create failed
  revoke_confirm_prefix: Revoke federation token
  revoke_confirm_suffix: "? Primary calls using it will start returning 401."
  toast_token_revoked: Token revoked
  toast_revoke_failed: Revoke failed
  toast_token_copied: Token copied
  toast_mapping_updated: Mapping updated; redeploying stacks
  toast_mapping_created: Mapping created; redeploying stacks
  toast_save_failed: Save failed
  delete_mapping_confirm_prefix: Delete mapping
  delete_mapping_confirm_suffix: "? Stacks referring to it will lose the host on their next redeploy."
  toast_mapping_removed: Mapping removed; redeploying stacks
  toast_delete_failed: Delete failed
  toast_mesh_reapplied: Mesh re-applied
  toast_apply_partial: Apply partially failed
  toast_apply_failed: Apply failed
  disable_confirm: Disable mesh? This will bring wg0 down on every peer and clear the Pier-side allocation.
  disable_uninstall_confirm: Also uninstall pier-net-helper / wireguard packages on each peer? (OK = uninstall, Cancel = keep installed)
  toast_mesh_disabled_prefix: Mesh disabled —
  toast_mesh_disabled_suffix: peer(s) had issues
  toast_mesh_disabled: Mesh disabled
  toast_disable_failed: Disable failed
  toast_mesh_settings_saved: Mesh settings saved

# Server detail (servers/detail.html).
servers_detail:
  title: Server Detail
  rename_hint: Double-click to rename
  save: Save
  testing: Testing...
  test_connection: Test Connection
  open_panel: Open panel
  promote_to_standalone: Promote to standalone
  remove: Remove
  promote_modal_title: "Promote agent → standalone pier-core"
  promote_modal_desc_pre: "Exports a bundle with all services, domains, environment variables, backup schedules and global configuration currently associated with "
  promote_modal_desc_post: ". Apply it on that server so it can run as a full Pier panel instead of a lightweight agent."
  promote_heads_up: Heads up
  promote_warn_core: Target server must run pier-core (same major version). If it only has pier-agent, install pier-core first.
  promote_warn_empty_db: Target database must be empty (no existing services). The import refuses to overwrite.
  promote_warn_secrets: "Users / sessions / secrets from this panel are <em>not</em> exported — create an admin on the new panel when it starts."
  promote_automatic_title: Automatic (recommended)
  promote_automatic_desc: Core pushes the bundle to the agent, which downloads pier-core, imports the bundle, installs a systemd unit, stops itself and starts the new pier-core. You watch the log on the target.
  promote_detach_label: Remove this server from my panel after the agent accepts the promotion
  promoting: Promoting...
  promote_now: Promote now
  log_label: "Log:"
  manual_alternative: Manual alternative
  manual_step1: 1. Download the bundle
  download_bundle: Download bundle (.json)
  manual_step2: 2. Apply on target server
  close: Close
  connection_info: Connection Info
  host: Host
  port: Port
  agent_token: Agent Token
  agent_token_prefix_hint: Only the prefix is shown. The full token lives in the agent's systemd unit.
  bootstrap_pending: Bootstrap pending
  os: OS
  hostname: Hostname
  uptime: Uptime
  location_and_docker: "Location & Docker"
  location: Location
  docker_version: Docker Version
  containers: Containers
  running: running
  total: total
  images: Images
  last_heartbeat: Last Heartbeat
  local: Local
  never: Never
  system_metrics: System Metrics
  unavailable: Unavailable
  refreshes_every_5s: Refreshes every 5s
  cpu_usage: CPU Usage
  memory: Memory
  disk: Disk
  containers_plural: containers
  container_singular: container
  no_containers: No containers found on this server
  col_name: Name
  col_image: Image
  col_state: State
  col_status: Status
  bootstrap_waiting: "Waiting for the remote host to run the install command. The bootstrap token is one-shot and was shown at creation time — we don't keep a copy."
  bootstrap_expires_pre: "Bootstrap expires "
  bootstrap_expires_post: ". If you lost the install command, delete this agent and create it again to issue a fresh bootstrap."
  federation_token: Federation token
  paired: paired
  not_paired: not paired
  federation_token_desc: "Paste the plaintext token this peer minted under <span class=\"font-mono\">/servers</span> → Federation Tokens → <em>New Token</em>. Pier stores it as plaintext (same trade-off as <span class=\"font-mono\">agent_token</span>); we never re-display it after saving."
  working: "Working…"
  clear: Clear
  agent_credential: Agent Credential
  rotate_hint: Generate a new long-term token, push it to the agent, and respawn the agent service.
  rotate: Rotate
  rotating: "Rotating…"
  agent_credential_desc: Long-term token in use (only the prefix is shown; the full value lives in the agent's systemd unit on the remote host).
  version: Version
  last_rotated: Last rotated
  never_rotated: Never rotated
  err_load_server: Failed to load server
  err_metrics_unavailable: Metrics unavailable
  err_server_offline: Server is offline
  err_fetch_metrics: Could not fetch metrics
  toast_renamed: Server renamed
  toast_agent_online: Agent is online
  confirm_promote: "Promote %{name} to standalone pier-core?"
  confirm_promote_detail: The agent will stop itself and pier-core will take over on the remote host.
  toast_promotion_started: Promotion started on target server
  err_bundle_export: Bundle export failed
  toast_bundle_downloaded: Bundle downloaded
  prompt_confirm_removal: "Type \"%{name}\" to confirm removal:"
  toast_name_mismatch: Name does not match
  toast_server_removed: Server removed
  expires_soon: soon
  expires_now: "now (expired — delete and recreate the agent)"
  expires_lt_minute: in less than a minute
  expires_1_minute: in 1 minute
  expires_n_minutes: "in %{count} minutes"
  toast_federation_paired: Federation paired
  toast_save_failed: "Save failed:"
  confirm_unpair: Unpair federation? Primary will lose the ability to deploy/restart stacks on this peer until you paste a fresh token.
  toast_federation_unpaired: Federation unpaired
  toast_unpair_failed: "Unpair failed:"
  confirm_rotate: Rotate the agent token now? The agent will be respawned by systemd and the old token will stop working immediately.
  toast_token_rotated: Token rotated; agent is restarting
  toast_rotate_failed: "Rotate failed:"

# Test-only fixtures exercised by the src/i18n unit tests. Not referenced by any
# template; kept here because the unit tests share this compiled-in catalog and
# need a stable `%{name}` interpolation sample (no section-1 screen has one yet).
test_fixtures:
  plain: Spike works
  interp: "Hello, %{name}"
